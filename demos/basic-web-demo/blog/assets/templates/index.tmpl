<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
  </head>
  <body>
    <h1>{{ .SomeFields }}</h1>
    {{range .Values}}
    {{ .}} <!-- should print 1 2 3 4 5 -->
    {{end}}

    {{ if .Data}} <!-- this happens if data != "" -->
    {{else}}
    {{end}}
  </body>
</html>
